{
  "name": "@mcp-arena/functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "yarn build && firebase deploy --only functions",
    "serve": "yarn build && firebase emulators:start --only functions,firestore,storage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/node": "^22.18.0",
    "typescript": "^5.9.2"
  }
}
