{
  "name": "@mcp-arena/functions",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "echo 'No build step needed for JavaScript functions'",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions,firestore,storage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "zod": "^3.24.1"
  }
}
